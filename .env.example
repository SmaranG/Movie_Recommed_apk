# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY=""

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL=""

# Custom Firebase Configurations for local VS Code runs
# Copy this file to a new file named `.env` in your project root and replace these with your own Firebase Project keys.
# You can get these keys from your Firebase Console under Project Settings > General > Your Apps.
VITE_FIREBASE_PROJECT_ID=""
VITE_FIREBASE_APP_ID=""
VITE_FIREBASE_API_KEY=""
VITE_FIREBASE_AUTH_DOMAIN=""
VITE_FIREBASE_STORAGE_BUCKET=""
VITE_FIREBASE_MESSAGING_SENDER_ID=""

# If your Firestore database is a custom database (not the default "(default)" one), specify its ID here.
# Otherwise, leave it empty or delete this line to use your default Firestore database.
VITE_FIREBASE_DATABASE_ID=""

